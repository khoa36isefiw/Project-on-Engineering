import { Typography } from '@mui/material';

function Order() {
    return (
        <Typography
            sx={{
                mt: '200px',
                ml: '200px',
                textAlign: 'center',
            }}
        >
            Order
        </Typography>
    );
}

export default Order;
