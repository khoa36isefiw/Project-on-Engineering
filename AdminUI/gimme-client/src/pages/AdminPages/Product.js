import { Typography } from '@mui/material';

function Product() {
    return (
        <Typography
            sx={{
                mt: '200px',
                ml: '200px',
                textAlign: 'center',
            }}
        >
            Product
        </Typography>
    );
}

export default Product;
